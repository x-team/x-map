{% if location %}
<http://map.x-team.com/profile/user.id|{{ user.firstName }} {{ user.lastName }}> (@{{ user.slackId }}) has just updated their location to {{ location }}.
{%  else %}
<http://map.x-team.com/profile/user.id|{{ user.firstName }} {{ user.lastName }}> (@{{ user.slackId }}) has just updated their location.
{% endif %}